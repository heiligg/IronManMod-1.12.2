package com.example.ironmanmod;

public class CommonProxy {
    // Only called on the server; nothing to render here
    public void registerRenderers() {
        // No rendering on server
    }
}
